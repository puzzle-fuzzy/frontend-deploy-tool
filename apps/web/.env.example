# Frontend Environment Variables

# Public Base URL for deployment links
# Use this when the frontend and backend are served from different origins
# Example: VITE_PUBLIC_BASE_URL=https://deploy.example.com
# Leave unset to use the same origin as the frontend (default behavior)
# VITE_PUBLIC_BASE_URL=
